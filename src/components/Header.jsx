import React from "react";

function Header(props) {
  return (
    <div>
      <h1>Simple Clock Setting App </h1>
      <p>
        Enter the hour and the minute you would like to set the clock to. Then
        click the button
      </p>
    </div>
  );
}

export default Header;
